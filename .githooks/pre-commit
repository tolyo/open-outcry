#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

if ! git diff --cached --name-only --diff-filter=ACMR | grep -E '(^|/)(Makefile|go\.mod|go\.sum)$|\.go$|\.sql$' >/dev/null; then
	exit 0
fi

make precommit
