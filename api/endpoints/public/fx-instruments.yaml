get:
  tags:
    - public
  summary: Fx instrument list
  description: Returns list of available fx instruments
  operationId: getFxInstruments
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "../../models/instrument.yaml#/components/schemas/FxInstrumentList"
    "404":
      description: Not found
