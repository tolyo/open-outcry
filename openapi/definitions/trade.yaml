components:
  schemas:
    TradeList:
      type: array
      items:
        $ref: '#/components/schemas/Trade'

    Trade:
      type: object
      properties:
        id:
          $ref: './shared.yaml#/components/schemas/Id'
