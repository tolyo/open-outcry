components:
  schemas:
    TradingAccountList:
      type: array
      items:
        $ref: '#/components/schemas/TradingAccount'

    TradingAccount:
      type: object
      properties:
        id:
          $ref: ./shared.yaml#/components/schemas/Id
