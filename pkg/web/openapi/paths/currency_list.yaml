get:
  tags:
    - currencies
  summary: currencies list
  description: Returns list of available currencies
  operationId: getCurrencies
  responses:
    "200":
      description: currencies list
      content:
        application/json:
          schema:
            $ref: ../definitions/currency_list.yaml
    "500":
      description: Error
